# Copy to .env.local. Local-only; no cloud accounts.
# App connects as the RLS-enforced fuse_app role:
DATABASE_URL=postgres://fuse_app:fuse_app@localhost:5432/fuse
# migrate.mjs connects as the admin/superuser to reset + seed:
DATABASE_URL_ADMIN=postgres://postgres:postgres@localhost:5432/fuse
